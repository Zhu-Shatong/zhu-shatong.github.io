# Local preview matching the old template path:
# http://127.0.0.1:4000/academic-homepage/
#
# Build/serve with:
# bundle exec jekyll serve --config _config.yml,_config_local.yml --host 127.0.0.1 --port 4000
url: "http://localhost:4000"
baseurl: "/academic-homepage"
